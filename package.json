{
  "name": "plandoskonaly",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install && cd ..",
    "postinstall": "cd frontend && npm run build && cd .."
  }
}
