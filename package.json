{
  "name": "plandoskonaly",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix frontend",
    "postinstall": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "vite": "^4.3.4"
  }
}
