{
  "name": "plandoskonaly",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix frontend",
    "postinstall": "PATH=$PATH:./frontend/node_modules/.bin npm run build --prefix frontend"
  }
}
